import styled from "styled-components";

export const Layout = styled.div`
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  section {
    position: relative;
  }
  ul {
    display: flex;
  }
  ul.second {
    position: absolute;
    top: -30px;
    left: 54px;
  }
`;
